# No environment variables are required. The Mancala Solver runs entirely
# client-side (the solver executes in a Web Worker in the browser); the
# server here only serves the built static app and a lightweight health
# check endpoint.

# PORT: optional. Defaults to 3000 locally; most hosts (Render, Railway,
# Fly, etc.) inject this automatically.
PORT=3000
